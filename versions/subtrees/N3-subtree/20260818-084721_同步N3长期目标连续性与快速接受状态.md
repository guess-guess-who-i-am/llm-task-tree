# LLM Task Graph Subtree

> Fold root: N3
> v2 并行试跑包

## N3 - 让 Codex 同步维护任务图
- Position: 2052,950
- Size: 400,420
- Completion: 进行中
- Problem: 多个 Agent 共用一棵树时，如何避免把全局 Next 误当作各自目标并互相覆盖？
- Approach:
  - 模型在10秒总预算内生成2–4个分支；超时或重启中断则立即生成可审核保守草案。
  - 计划阶段只拒绝精确保留路径；运行后按实际改动拦截共享状态和越界文件。
  - worker 在独立 worktree 写入并测试；Codex 接受任务即回写入口，服务中断后保留已完成分支并续跑汇总。
  - 首屏只显示编号、自然名称、状态和一句结果；完整指令、写集与测试按需展开。
- Input: 当前任务树、本轮目标、可审核并行草案，以及开始与结束两次用户决定。
- Output: 带目标的自动规划、隔离并行、严格接受门禁、失败分支局部重跑与接受后节点同步。
- Metrics: 草案阶段零写入；点击后立即显示准备状态，Codex 接受任务后即出现入口；记录快照耗时并与86秒基线比较；受保护文件实际改动必拒绝；失败分支只重跑自身；审核前主工作区不变。
- Notes:
- CodeLoc:
  - server/codex-run.js # 规划总预算与会话取消
  - server/codex-coordinator.js # 规划、降级与孤儿恢复
  - server/parallel-worktree.js # 实际保护路径检查
  - public/app.js # 审核信息折叠
- CurrentResult: 真实N3运行已自动完成3个分支、汇总及6/6集成测试，并进入可接受的结束审核；Codex 接受分支即显示任务入口，脏工作区准备由86秒降至16.1秒，等待期即时显示状态。首屏仅保留编号、自然名称、状态和一句结果，完整指令与技术元数据折叠；本地、插件、Linux及共享安装回归通过。并行闭环已可用，但自动分解能否长期扣住真实业务目标仍待使用验证，N3整体目标暂不能宣称达到。
- RootCauseAnalysis: 看似未启动并非 worker 未运行，而是脏工作区快照需86秒且 threadId 只在完成后落盘；信息过多则因计划摘要、完整指令和技术元数据同时占据首屏，名称又直接复用 planner 抽象术语。现以增量索引、接受即回写及渐进展开分别处理。
- CaseStudy:
  - case 1: 主工作区已有未提交和未跟踪文件 → 快照保留用户基线，审核前主目录不变。
  - case 2: 运行期间用户再次修改同一文件 → 接受被阻断，不静默覆盖。
- NextIdea: 在下一次真实业务并行中记录开始审核时改动的分支名称、任务和依赖，据此判断是否继续优化自动分解。
- SelectedSkills: codex:skill-creator

# GraphState

- Current: N3
- Next: N3
- NextPlan: v2 findings 已完成；可选 sync-stub 或真实任务试跑 AGENTS 协议

# Edges
