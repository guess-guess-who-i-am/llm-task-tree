# N2 · 可视化图谱交互

## 2026-08-17 节点左右栏尺寸收敛

1. 窄节点的操作栏自动改为单行 29px 高；宽节点使用两行 55px 高，避免右侧方块撑高标题区。
2. “问题 / 思路 / 结果”标签轨道按节点宽度保持 40–52px，正文宽度始终超过标签轨道 3 倍。
3. 14 个节点核心摘要回归通过；内容自适应回归得到宏观 6、细节 8、手机 9 种尺寸，标题裁切和节点重叠均为 0。

证据：`scripts/test-node-core-summary-ui.mjs`、`artifacts/inspect-node-columns.png`。

## 2026-08-17 节点按内容自适应

1. 默认摘要和宏观层不再直接使用 Markdown 中保存的固定 `Size`；宽度按标题与问题、思路、结果的文字量分档，高度由浏览器实际内容测量。
2. 卡片保留最小和最大尺寸，短节点收紧，长节点获得更多空间；详文和编辑模式仍保留手动尺寸。
3. 自动排版按细节层最大高度预留，解决宏观层排紧后放大重叠的问题；连线、画布边界和缩放读取实际显示尺寸。
4. 当前树实测宏观 6 种、细节 10 种、手机 9 种尺寸；标题裁切 0，细节节点重叠 0。
5. `scripts/test-content-sized-nodes.mjs`、树优先工作台、焦点透镜和项目总览回归通过；正式源、共享 kit、插件运行时、Codex 缓存和本机模板已同步。

证据：`artifacts/content-sized-nodes-macro.png`、`artifacts/content-sized-nodes-detail.png`、`artifacts/content-sized-nodes-mobile.png`。

## 最新交付：焦点透镜只改变关系图呈现

1. 透镜入口上收为顶部唯一按钮，打开当前选中节点并可再次关闭；所有节点卡片都不再重复显示入口。
2. 透镜中央节点首屏可直接编辑「让 Agent 继续做什么」；它写回同一个 `NextIdea`，不是独立草稿。
3. 增加设为当前、设为下一步、加入/移出执行链、完成状态和完整编辑入口；普通节点与透镜复用同一组状态函数。
4. 「保存并让 Codex 继续」会先把透镜节点设为 Next、等待任务树落盘，再调用 Next 预设，避免执行其它节点。
5. 焦点透镜只属于关系图：切到执行流程时自动让开，切回关系图后恢复原焦点和编辑内容；顶部工具、左右面板和底部执行链保持可用。
6. 开发源、共享 kit、插件运行时三套前端哈希一致；桌面和手机真实浏览器验收通过。

## 本轮根因

首次浏览器重放看不到编辑器，是因为运行服务从 `llm-task-tree-kit/public/` 提供页面，而修改只落在根目录开发源。已定点同步三份前端，未触碰并发修改的 `scripts/mcp-server.mjs` 与 `server/codex-run.js`。

## 本轮证据

- `scripts/test-focus-lens-integration.mjs`
- `artifacts/focus-lens-desktop.png`
- `artifacts/focus-lens-mobile.png`
- 公开分发：234 个文件、4.20 MiB，本机路径和疑似凭据均为 0。

## 本步做了什么

1. 图谱顶栏增加 **摘要/详文** 切换（默认摘要）：卡片只显示标题、完成态、截断问题、一行结果；思路/评价/结果/根因放进「详情」。
2. 摘要模式下卡片高度从保存的 ~720 压到约 200–420，避免空白撑满画布。
3. 精炼 `task-tree.md` 与 `subtrees/N3|N6|N7-subtree.md`（备份在 `versions/202607220946224_*`）。
4. `build-kit` + 一键更新：25 项目 stub 刷新，failed=0。
5. 自动整理改为按每层左右轮廓避碰：没有后代的兄弟节点不再为别人的整棵子树预留宽度；卡片横向间距从 16–32 缩到 8–18。

## 测量

| 对象 | 前 | 后 | 变化 |
|------|----|----|------|
| 主树 bytes | 27366 | 14365 | **-47.5%** |
| 主树 longLines(>240) | 5 | 0 | -100% |
| N7 子树 bytes | 9196 | 1833 | **-80%** |

## 验证

Ctrl+F5 后：顶栏「摘要」为按下态；节点卡片显著变矮；点「详情」可展开；点「详文」可恢复旧密度。
