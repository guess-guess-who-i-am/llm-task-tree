# LLM Task Graph

> 这个文件是大模型和前端共同维护的任务图。节点保存问题空间，边保存节点之间的关系；每条边只连接两个节点。
## ROOT - 建立可共享的大模型任务上下文
- Position: 1777,70
- Size: 400,720
- Completion: 进行中
- Problem: 人类看不到大模型隐性上下文，难以判断任务推进到哪里、是否偏航、卡点是什么。
- Approach: 用 Markdown 作为共享存储，用可视化前端把任务图展示出来，并允许直接编辑节点、边和批注。
- Input: 长对话、任务目标、模型阶段性发现、用户判断。
- Output: 可维护的任务图、节点内容、边关系、评价指标和批注。
- Metrics: 人类能在 30 秒内判断当前焦点、关系结构、阻塞点和下一步。
- Notes: 初始目标是让上下文外置，而不是让模型每轮输出长篇总结。
- CodeLoc:
- CurrentResult:
- RootCauseAnalysis:
- CaseStudy:
- NextIdea:
- SelectedSkills:

## N1 - 设计 Markdown 节点格式
- Position: 2633,950
- Size: 400,720
- Completion:
- Problem: Markdown 需要同时适合人读、模型写、前端解析。
- Approach: 使用固定字段列表，节点标题包含 ID 和标题，边关系放到独立 Edges 区域。
- Input: 节点 ID、标题、位置、问题、思路、输入、输出、指标、批注。
- Output: 一个稳定的 Markdown schema。
- Metrics: 手写不费劲；前端可以解析并保存。
- Notes: 节点不再承担状态和父子关系，关系交给边表达。
- CodeLoc:
- CurrentResult: Schema 保留 Input/Output 等字段；§3 已加 **推理图原则**（Problem=子问题、Metrics=现实意义+测法、CurrentResult=数字/负结果、NextPlan=可执行、边写依赖含义、公式链拆分）。同步 graph-quality.md、AGENTS.merge 一行、task-tree-grill 停止条件。kit 源：`llm-task-tree-kit/AGENTS.task-tree.md`；一键更新会推到各项目 prompt。
- RootCauseAnalysis: 字段膨胀的根因是任务状态、执行结果、失败原因和案例都写进同一节点后长期累积；应保留字段，但每轮只写 1-3 句结果和真正根因，下一步思路放 NextIdea/NextPlan，不塞进 RootCauseAnalysis。只写路径的根因是早期 §3 把「路径+样例均可」写成合法格式，导致 Agent 偷懒只列文件名。
- CaseStudy: case: 用户要求“节点里面删掉输入输出”→ 应从主卡片移出而非删字段。 case: 用户要求“不要只写路径，要在 I/O 里看到例子”→ 协议与 UI 从「路径+样例均可」改为「默认贴内容，路径仅作补充」。
- NextIdea:
- SelectedSkills:

## N2 - 实现可视化图谱编辑器
- Position: 3258,950
- Size: 670,819
- Completion:
- Problem: 需要一眼看图，并能直接在图上修改节点和关系。
- Approach:
  - 本地 Web 应用以 task-tree.md 为单一数据源，支持节点、边、I/O、SelectedSkills 和 GraphState 的可视化编辑与自动保存。
  - 交互采用单击选择/拖动、双击编辑；节点连接点、边标签控制点、浮层标题栏拖动用于减少误操作。
  - 关系图支持当前/下一步路径高亮、树形排版、版本树回退，以及项目内 launcher 自动查找或启动正确端口。
  - I/O 预览和 skill 推荐作为选中节点的侧向浮层；I/O 中的真实文件路径会显示有界内容快照，服务端保存时保护未知字段。
  - SVG 导出截取真实 DOM；导出前临时撑高节点并排版，导出后恢复原布局不写盘。
- Input:
  - `## N2 - 实现可视化图谱编辑器`  # 当前要维护的图谱编辑器节点
  - `GraphState.NextPlan: 重新打开任务图（Ctrl+F5）后验证：节点 × 在九宫格右上角；点 ⤓ 关系图/流程图 下载 SVG 并用 Inkscape/浏览器打开确认文字完整。`  # 当前验证入口
  - `AGENTS.md`  # I/O 文件快照测试用的原始协议文件
  - `用户需求：一眼看图，并能直接在图上修改节点和关系；I/O 里要能看到原始文件长什么样。`  # N2 的核心输入约束
- Output:
  - `关系图 | 执行流程`  # 主应用顶栏两个视图入口
  - `×`  # 节点九宫格右上角删除按钮，当前 NextPlan 待验证
  - `⤓`  # 关系图/流程图 SVG 下载按钮，当前 NextPlan 待验证
  - `GET /api/file?path=AGENTS.md&preview=1&maxChars=120 -> size=32676,truncated=true`  # I/O 文件快照接口实测结果
- Metrics:
  - Ctrl+F5 后能编辑节点/边/I/O/SelectedSkills 并自动落盘，且不产生前端输入噪声版本。
  - 关系图可一眼看到 Current/Next 路径；树形排版后节点不重叠，边标签不被节点遮挡。
  - 分别点击关系图/流程图 `⤓`，用 Inkscape 或浏览器打开 SVG，确认文字完整且布局与界面一致。
- Notes:
  - 当前范围仍是本地单人使用，不处理多人冲突合并。
  - 本轮未改变执行顺序；`scripts/project.json` 与 `scripts/run.json` 不需要同步。
- CodeLoc:
- CurrentResult: 图谱编辑器已支持节点/边编辑、自动保存、版本回退、I/O 预览、skill 推荐、项目 launcher、关系图与流程图入口。I/O 预览现在会识别字段里的文件路径并异步显示内容快照；实测 `AGENTS.md` 预览返回 32676 字节、截断=true、缺失文件返回 404。当前待验：Ctrl+F5 后确认节点 `×` 位置和关系图/流程图 `⤓` SVG 文字完整。
- RootCauseAnalysis: N2 变长的根因是把已完成的交互迭代、bug 过程、设计理由和案例都继续追加在同一节点里；当前有效状态应只保留功能面、待验证点和少量根因。只在 I/O 写路径仍然看不见原始数据，根因是前端原先没有把路径解析成有界文件快照。SVG 导出需要截真实 DOM，是因为重绘式导出容易与界面颜色、按钮、高亮和文字布局不一致。
- CaseStudy:
  - case 1: 左侧目录树+右侧编辑器不符合“看关系”的任务；改为知识图谱式画布后，节点和边成为第一视图。
  - case 2: Input/Output 放在主节点会让图谱难扫描；移到选中节点旁的预览框后，结构和真实样例可分层查看。
  - case 3: 已打开旧前端自动保存会丢弃未知字段；服务端保存路径必须保护字段，不能完全信任客户端提交完整 schema。
- NextIdea: 下一步可以把 launcher 做成可复制模板，或者增加“创建桌面快捷方式”的脚本。
- SelectedSkills:

## N4 - 分析本地 skill 仓库与自动调用机制
- Position: 130,950
- Size: 460,720
- Completion:
- Problem: 用户在本项目下放入多个 skill 仓库，需要知道每个 skill 的用途、特点、调用方式，以及如何让大模型自动选择合适 skill。
- Approach: 扫描 CL4R1T4S、gill_with_doc、scientific-agent-skills 中的技能定义与说明，结合 tool/skill routing 相关研究，形成可落地的自动选择机制。
- Input: 全局 skill 根目录（~/.codex/skills、~/.agents/skills、~/.orchestra/skills）、当前节点 SelectedSkills、GraphState.NextPlan、节点 Problem/Approach/Metrics、skill-routing-log.md。
- Output: 中文 skill 推荐面板、SelectedSkills 落盘字段、AGENTS.md 执行协议、skill-routing-log.md 实际使用记录、`dist/skills-memory-transfer-kit` 迁移包。
- Metrics: 能列全本地 skill；能说明每类 skill 何时使用；能给出可实现的路由流程、置信度策略和评估办法。
- Notes: 这是一项新的分析任务，和已有任务图工具实现相关但不等同于前端功能开发。
- CodeLoc:
- CurrentResult: 已整理 `dist/skills-memory-transfer-kit`：184 个标准 `SKILL.md`（任务树 3、工程协作 34、科研 147），包体 45.82 MB，含中文 README、记忆系统说明、路由策略和 `metadata/skill-index.json`。校验：gill/scientific 文件数与源目录一致；CL4R1T4S 仅列为 reference-only。
- RootCauseAnalysis: 不能把“本地有很多 skill”直接等同于“模型会正确自动调用”；根因是 skill 数量大、粒度不一、信任等级不同且触发边界重叠。迁移包采用先索引、再召回、最后 progressive disclosure 加载的方式，避免把全部 `SKILL.md` 直接塞进上下文。
- CaseStudy:
  - case 1: CL4R1T4S 含提示词档案与注入风险文本 -> 若进自动池会污染执行 -> 迁移包只放 reference-only 说明。
  - case 2: scientific-agent-skills 有 147 个 skill -> 全量自动启用会造成噪声 -> 默认按任务 opt-in。
- NextIdea: 可以继续做 skill 启用/隐藏清单，减少推荐噪声；也可以增加一个“复制给 Codex 的下一步执行提示”按钮，把 NextPlan、SelectedSkills 和当前节点摘要组合成短提示。
- SelectedSkills: codex:skill-creator

## N6 - 节点内多模型协作
- Position: 40,40
- Size: 0,720
- Completion: 进行中
- Problem: [子树已折叠] → subtrees/N6-subtree.md
- Approach:
- Input:
- Output:
- Metrics:
- Notes: v2 并行试跑；AssignedTo=worker-N6；合并请 UI ⊞ 展开。
- CodeLoc:
- CurrentResult: 节点多模型对话改为按「轮」持久化到 model-node-conversations.json（GET/PUT/DELETE /api/model-conversations）；UI 与侧栏一致：折叠/删除/摘要/重启可见；不写 task-tree。勾选侧栏「送入模型协作」时 run 前 server 自动 searchRetrieval（LLM 提关键词），模型仍可 search JSON 追加检索；每轮显示 autoRetrieval 行。
- RootCauseAnalysis: 对话只在内存 modelConversations、重启丢失；检索依赖模型主动输出 search JSON 且常不触发。根因是无持久化 API 使用 + 无强制预检索步骤。
- CaseStudy:
- NextIdea: 重启服务+Ctrl+F5；某节点跑一轮后关浏览器再开，确认历史仍在；看「自动检索：… · N 条」。
- SelectedSkills:
- Folded: true
- SubtreeFile: subtrees/N6-subtree.md
- SubtreeCount: 1

## N7 - 集成 Markdown 知识库检索面板
- Position: 520,40
- Size: 432,755
- Completion: 进行中
- Problem: [子树已折叠] → subtrees/N7-subtree.md
- Approach:
- Input:
- Output:
- Metrics:
- Notes: v2 并行试跑；AssignedTo=worker-N7；合并请 UI ⊞ 展开。
- CodeLoc:
- CurrentResult:
  - 诊断确认旧 reindex 串行：每轮只 `await embedTexts` 一个 64 文本 batch。
  - 已改为可配置并发：默认 `batchSize=64`、`concurrency=40`，支持 env/config 覆盖。
  - 验证：`node --check server.js` 与 kit `server.js` 通过；一键更新 17 项目 failed=0。
  - 检索多样性（2026-07-09）：`searchKnowledge` 新增 `diversifyResultsByDocument`——先取 topK×10 候选池，再按文件 path 轮询，默认每篇最多 1 段；top 20 可覆盖最多 20 篇。配置见 `knowledge-config.json` → `retrieval`。
- RootCauseAnalysis: embedding 慢的根因是重建索引阶段按 batch 串行调用 `/embeddings`；provider 空闲时吞吐被单请求延迟限制。当前改为 batch 级并发，若供应商限流可用 `KNOWLEDGE_EMBEDDING_CONCURRENCY` 降级。检索结果篇数少的根因是纯 top-K 按 chunk 分数排序，长文多 chunk 会占满 slot；联网已有 `diversifyResultsByHost`，本地知识库此前无 per-doc 上限。
- CaseStudy:
- NextIdea: 重启任务图服务后试同一问题，看参考文献摘要是否显示「覆盖 N 篇」；若单篇仍不够可设 `maxChunksPerDoc: 2`，若仍偏少可增大 `candidatePoolMultiplier`。
- SelectedSkills:
- Folded: true
- SubtreeFile: subtrees/N7-subtree.md
- SubtreeCount: 1

## N10 - 执行顺序视图（Scratch 模块流原型）
- Position: 1200,1830
- Size: 520,640
- Completion: 已完成
- Problem:
- Approach:
  - 已合并进主应用：`public/flow-view.js` + `server/flow-script.js`；任务图谱顶栏 **关系图 | 执行流程**。
  - 脚本落盘 `scripts/project.json`、`scripts/run.json`；版本 `scripts/versions/`。
  - **执行顺序以 scripts 为准**；关系图折叠不影响已保存脚本；catalog 合并主树 + 全部 `subtrees/*.md`。
  - Agent 规则：`scripts/README.md` + AGENTS.md §Execution flow scripts。
- Input: `task-tree.md`、`subtrees/*.md`、GraphState、边。
- Output: 主应用执行流程视图 + `scripts/*.json`。
- Metrics:
- Notes: `prototype/swimlane-view/` 保留作参考；正式入口为打开任务图.cmd。
- CodeLoc:
- CurrentResult: 步骤详情 Inspector（flow-view.js:435 selectStepNode）与 GET /api/flow-script/drift（computeFlowDrift）已上线；scripts/steps/ 为 12 个 task 块写入 audit 包。project.json 重建为 13 blocks（含 ST-P1/P2），focusId=N2；总览见 scripts/flow-overview.zh.md。
- RootCauseAnalysis: 原型脚本在 localStorage 导致 Agent 不可见；折叠与流程图耦合导致子节点从调色板消失。
- CaseStudy:
- NextIdea: 链式推进时可选自动同步 run.json。
- SelectedSkills:

## N9 - 可移植任务树 Kit 打包
- Position: 3180,1830
- Size: 520,640
- Completion: 已完成
- Problem: 其他项目也要用同一套任务图能力，且各自维护独立的 task-tree.md；复制过去的手续要少，已有 AGENTS.md 的项目不能被覆盖。
- Approach:
  - 改 server/launcher 支持 task-tree.config.json 的 projectRoot，kit 放子目录、数据放项目根。
  - 导出 llm-task-tree-kit/ 文件夹，复制到任意项目后运行 install.ps1 即可。
  - install 创建 task-tree.starter.md、合并 AGENTS.md 标记块、追加 gitignore；完整协议保留在 AGENTS.task-tree.md。
  - 改前全量备份到 backups/snapshot_*。
- Input: 当前仓库 server.js、public/、AGENTS.md、skills/task-tree-grill、启动脚本。
- Output:
  - one-click-update.ps1 -> discovered 17 projects, stub-refreshed 17, prompts-synced 17, failed 0 # 本轮同步结果
  - install.manifest.json updatedAt=2026-07-04T21:18:00+08:00 # 共享 kit 安装记录
  - projects include E:\shusheng; E:\zongshu\paper_crawler_ai_awards; F:\empty-window; F:\OPSD # 发现范围样例
- Metrics: 复制 kit 到新项目 → install → 打开任务图 → 独立 task-tree.md；已有 AGENTS.md 只追加 llm-task-tree 块不覆盖。
- Notes: open-webSearch 等大依赖仍可选，不强制打进 kit。
- CodeLoc:
- CurrentResult:
  - 一键更新已重跑：build-kit 同步根目录到 `llm-task-tree-kit/`，npm/open-webSearch/右键菜单刷新均成功。
  - 项目发现 17 个；stub-refreshed=17，prompts-synced=17，failed=0。
  - 共享 kit 已包含 embedding `concurrency=40` 和 `.env.example` 配置说明。
- RootCauseAnalysis:
- CaseStudy:
- NextIdea: 新项目只需右键 Install 或放 task-tree.md 后一键更新；深层目录可写入 update-projects.txt 或 update-search-roots.txt。
- SelectedSkills:

## N3 - 让 Codex 同步维护任务图
- Position: 1000,40
- Size: 424,915
- Completion: 进行中
- Problem: [子树已折叠] → subtrees/N3-subtree.md
- Approach:
- Input:
  - `用户反馈：旧方法和新方法会一起留在树里，内容越来越多，人类看不过来。`  # 协议修订来源
  - `versions/`  # 历史回溯保存位置， live tree 不再承担审计日志
- Output:
  - `task-tree.md 是 compact current working state，不是 append-only history log`  # 新协议核心规则
  - `冲突时当前方法获胜；删除旧内容，不写“7月2日删除...” tombstone`  # 噪声清理规则
- Metrics:
- Notes: v2 并行试跑；AssignedTo=worker-N3；合并请 UI ⊞ 展开。
- CodeLoc:
- CurrentResult: 已同步精炼写树协议到 `AGENTS.md`、`llm-task-tree/AGENTS.task-tree.md`、kit 协议和 merge 模板：活树只保留当前有效方法，历史交给 `versions/`；字段设短预算；方法变化时要检查执行流程是否需要同步。子代理测试显示：N2/N3 无需继续改；已修复多端点边、N4 case 过多、Completion 混入 AssignedTo。
- RootCauseAnalysis: 树污染的根因是早期 prompt 把 `task-tree.md` 当成可追加的过程日志，模型为了“保留历史”把废弃方法、删除标注和新方案并排写入同一字段。正确策略是 live tree 保存当前结论和少量根因，旧版本由 `versions/` 回溯。
- CaseStudy:
- NextIdea: 下次触碰旧节点时先做局部精炼：删除 tombstone、过期方案和重复过程记录，只保留当前方法、证据样例和下一步。
- SelectedSkills:
- Folded: true
- SubtreeFile: subtrees/N3-subtree.md
- SubtreeCount: 1

## N5 - 处理任务树回溯后的文件漂移
- Position: 2099,1870
- Size: 523,640
- Completion: 已完成
- Problem: task-tree.md 可以通过版本树回溯，但代码、skill 文件、日志和其它产物不会自动回滚，导致“任务树说没做过、文件系统却残留做过的结果”。
- Approach:
  - 在 AGENTS.md 中明确 task-tree.md 是权威任务状态，聊天记忆、旧日志和残留文件只能作为证据。
  - 回溯后如果当前树没有记录某个产物，Codex 不能因为该产物存在就跳过任务；必须按当前 GraphState.Next 重新验证或重做。
  - 残留产物先视为 orphan artifact，不自动删除；只有当前节点要求时才复用、重写或清理。
- Input: task-tree.md、versions/、AGENTS.md、当前文件系统中的残留产物和 skill-routing-log.md。
- Output: AGENTS.md 中的 Rollback And Drift Rules、任务树中的漂移记录、下一步重新执行策略。
- Metrics: 回溯 task-tree.md 后，Codex 能明确按树继续推进；不会把不存在于当前树的旧节点当成已完成状态；文件漂移会被显式记录。
- Notes: 当前已发现一个具体漂移：task-tree.md 回溯后没有旧 N5，但 skills/task-tree-grill/SKILL.md 和 skill-routing-log.md 仍保留旧创建记录。
- CodeLoc:
- CurrentResult: 已在 AGENTS.md 新增回溯/漂移规则：task-tree.md 是权威任务状态；已有文件、聊天记忆和日志只能作为证据；回溯后遇到已存在产物也要按当前节点重新验证或重做。随后按该规则重新验证并修正了旧的 `skills/task-tree-grill/SKILL.md`，没有直接把旧 N5 记录当成完成状态。现在已完成建树 skill 的重构：主 `SKILL.md` 缩短为入口流程，新增 `references/interview-playbook.md`、`references/graph-quality.md`、`references/schema-template.md`、`references/rollback-drift.md`，并更新 `agents/openai.yaml`。
- RootCauseAnalysis: task-tree.md 的版本回退只改变任务图，不会事务性回滚整个工作区。根因是任务状态和文件产物使用不同存储机制，没有统一快照；因此必须用协议规定权威来源和漂移处理方式。建树 skill 之前的问题是把访谈流程、schema、质量标准、回溯处理都塞在一个 SKILL.md 里，触发后上下文负担偏重；根因是没有按 skill-creator/write-a-skill 的 progressive disclosure 原则拆分核心流程和细节参考。
- CaseStudy:
  - case 1: 旧的建树 skill 文件仍存在，但当前 task-tree.md 没有对应节点记录；如果直接认为它已完成，就会违背回溯后的任务状态。
  - case 2: skill-routing-log.md 记录旧 N5，不代表当前树仍有 N5；日志是审计证据，不是任务状态源。
  - case 3: 如果建树 skill 把所有规则都放在主文件里，模型每次触发都会加载过多内容；拆成 references 后，只有需要访谈、质量检查、schema 或回溯处理时才加载对应文件。
- NextIdea: 下一步用一个真实的新任务或长对话片段试运行 `task-tree-grill`，检查它能否只问一个关键问题并产出可执行的 3-7 节点树。
- SelectedSkills: codex:skill-creator, agents:write-a-skill

## ST-P1 - 子树试点A chain-step 审计
- Position: 660,1870
- Size: 380,280
- Completion: 已完成
- Problem: [子树] chain-step 是否泄露后续 Chain → subtrees/ST-P1-subtree.md
- Approach:
- Input:
- Output:
- Metrics:
- Notes: 折叠并行试点 stub；AssignedTo=worker-P1；详情在子树文件。
- CodeLoc:
- CurrentResult: 审计完成；见 docs/subtree-parallel/findings-P1.md；P0 redaction 已合入 server。
- RootCauseAnalysis:
- CaseStudy:
- NextIdea:
- SelectedSkills:
- Folded: true
- SubtreeFile: subtrees/ST-P1-subtree.md
- SubtreeCount: 1

## ST-P2 - 子树试点B 并行冲突梳理
- Position: 1620,1870
- Size: 380,280
- Completion: 已完成
- Problem: [子树] 多 Agent 写树/代码冲突点 → subtrees/ST-P2-subtree.md
- Approach:
- Input:
- Output:
- Metrics:
- Notes: 折叠并行试点 stub；AssignedTo=worker-P2；详情在子树文件。
- CodeLoc:
- CurrentResult: 冲突梳理完成；见 docs/subtree-parallel/findings-P2.md 与 coordinator-run-1.md。
- RootCauseAnalysis:
- CaseStudy:
- NextIdea:
- SelectedSkills:
- Folded: true
- SubtreeFile: subtrees/ST-P2-subtree.md
- SubtreeCount: 1

## N11 - 重构多树上下文与 Agent 维护闭环
- Position: 104,40
- Size: 0,720
- Completion: 进行中
- Problem: 如何避免单棵任务树和超长 AGENTS.md 持续膨胀，并让树与执行流程在真实使用中稳定同步？
- Approach:
  - 根 AGENTS.md 作为短路由入口，完整协议、原文和功能账本按需加载。
  - 用 registry 管理背景/方法/实验等独立树；只有 active method tree 绑定 flow、chain 和 subtree。
  - UI 显式切树/建树；版本、GraphState 缓存和模型上下文按 treeId 隔离。
  - NextPlan 仅作用户备忘；Agent 只执行 Next+NextIdea。树保存/Stop 自动同步 flow 状态并生成可归属的最小 step evidence。
- Input:
  - `单树混载背景、方法、执行证据；后端硬编码 task-tree.md`  # 用户报告与代码根因
  - `AGENTS.md 36299 bytes → 5950 bytes`  # Prompt 基线与结果
  - `Lost in the Middle / MemGPT / Reflexion / OPRO`  # 分层上下文证据
  - `docs/agent-context-research/prompt-audit/AGENTS.original.20260710.md`  # 冻结原 Prompt
- Output:
  - `task-trees.json + trees/background.md`  # 多树注册表与稳定背景树
  - `GET/POST /api/trees; GET/PUT /api/tree?tree=<id>`  # 隔离读写接口
  - `NextPlan=[用户备忘；禁止执行]; Next+NextIdea=唯一执行入口`  # 防止旧计划触发重做
  - `tree save → flow status sync; Stop → inferred-node step evidence`  # 自动维护
  - `scripts/test-multitree-maintenance.mjs + test-multitree-ui.py`  # 回归入口
- Metrics: 根 AGENTS.md <=8 KiB；active tree <=12 KiB/5-12 节点；20 个真实任务状态同步率 >=95%，默认上下文下降 >=60%，人工定位中位时间 <=30 秒。
- Notes:
  - GraphState 未改变；背景树永不进入 `scripts/project.json`。
  - flow 的 status 自动修复；missing/stale/order 仍由 Agent 处理，避免破坏人工编排。
  - active method tree 仍为 27,640 bytes；postflight 已持续告警，下一轮应把稳定架构详文迁出并保留方法 stub。
- CodeLoc:
- CurrentResult: NextPlan 已降级为不进入 Agent 执行上下文的用户备忘，链推进也不再自动改它；UI/导出明确区分用户备忘与 NextIdea。方法树保存会自动同步 flow status；Stop 根据前后树快照定位真实变更节点并补最小 step evidence。Node/API 8 项、Playwright 与 21/21 Prompt 覆盖均通过。
- RootCauseAnalysis: 重做旧任务是因为 NextPlan 同时承担用户备忘和执行指令，且本地缓存/链推进会反复刷新它；flow 漏维护则因树、flow、step 三套写入互不联动。当前只自动处理可确定的状态与证据，结构和顺序不确定时继续阻止而不猜。
- CaseStudy:
  - case 1: NextPlan 长期停留在旧 SVG 验证 → 模型可能重做 → 现在只显示为用户备忘并从 Agent 上下文遮蔽。
  - case 2: GraphState.Next=N2 但实际改 N11 → 不能把证据误记 N2 → Stop 对比树快照后归属真实变更节点。
- NextIdea: 建立 architecture tree，将 N1/N2/N9/N10 的稳定详文迁出为引用 stub，把 active method tree 压到 12 KiB 内。
- SelectedSkills:

# GraphState
- Current: N6
- Next: N6
- NextPlan: 重新打开任务图（Ctrl+F5）后验证：节点 × 在九宫格右上角；点 ⤓ 关系图/流程图 下载 SVG 并用 Inkscape/浏览器打开确认文字完整。

# Edges

## E1 - 格式支撑

- Endpoints: ROOT, N1
- LabelOffset:
- Label: 格式支撑
- Notes:

## E2 - 实现路径

- Endpoints: ROOT, N2
- LabelOffset:
- Label: 实现路径
- Notes:

## E3 - 协作规则

- Endpoints: ROOT, N3
- LabelOffset:
- Label: 协作规则
- Notes: 前端自动保存解决人改图后的落盘问题；AGENTS.md 解决 Codex 完成任务后的同步问题。

## E4 - skill 调用扩展

- Endpoints: ROOT, N4
- LabelOffset:
- Label: skill 调用扩展
- Notes: 用户希望把本地 skill 仓库纳入模型工作流，并进一步解决模型自动选择合适技能的问题。

## E6 - 多模型协作扩展

- Endpoints: N2, N6
- LabelOffset:
- Label: 多模型协作扩展
- Notes: N6 扩展 N2 的节点交互能力，让用户可在图谱节点内调用多个外部模型独立分析当前问题。

## E7 - 知识库检索扩展

- Endpoints: N2, N7
- LabelOffset:
- Label: 知识库检索扩展
- Notes: N7 扩展 N2 的页面入口和节点上下文，让本地 Markdown 知识库检索结果能进入节点级模型分析。

## E8 - 检索送入多模型协作

- Endpoints: N6, N7
- LabelOffset:
- Label: 检索送入多模型协作
- Notes: N7 的检索结果可进入 N6 的节点级多模型分析上下文；从原多端点 E7 拆出。

## E10 - 泳道原型

- Endpoints: N2, N10
- LabelOffset:
- Label: 泳道原型
- Notes: N10 在独立 prototype 中验证执行顺序视图，满意后合并回 N2。

## E9 - 可移植 Kit

- Endpoints: N2, N9
- LabelOffset:
- Label: 可移植 Kit
- Notes: N9 打包 N2 的主应用能力，供其它项目复制安装独立任务树。

## E11 - Kit 同步协议

- Endpoints: N3, N9
- LabelOffset:
- Label: Kit 同步协议
- Notes: N9 需要同步 N3 的 Agent 协议到 kit stub 和 merge 模板；从原多端点 E9 拆出。

## E5 - 回溯一致性约束

- Endpoints: N3, N5
- LabelOffset: -11,257
- Label: 回溯一致性约束
- Notes: N5 是 N3 的协议补充，解决 task-tree.md 回溯后文件系统残留导致 Codex 误判任务已完成的问题。

## E-ST-P1 - 子树试点A

- Endpoints: ROOT, ST-P1
- LabelOffset:
- Label: 子树试点A
- Notes:

## E-ST-P2 - 子树试点B

- Endpoints: ROOT, ST-P2
- LabelOffset:
- Label: 子树试点B
- Notes:

## E12 - 从长提示改为分层上下文与机械门禁

- Endpoints: N3, N11
- LabelOffset:
- Label: 从长提示改为分层上下文与机械门禁
- Notes: N11 替代继续扩写 N3 协议的方向；若该结论错误，AGENTS-lite 可能遗漏必要约束，需由 A/B 回归验证。

## E13 - 区分项目流程与 Agent 生命周期

- Endpoints: N10, N11
- LabelOffset:
- Label: 区分项目流程与 Agent 生命周期
- Notes: `project.json` 保留领域任务顺序；拟新增固定 agent-loop 负责每轮 step evidence、tree-lint 和 drift，不再要求模型靠记忆维护。
