# LLM Task Graph

> 活动方法树只保存会改变下一动作的当前状态；稳定背景和实现架构分别见 trees/background.md 与 trees/architecture.md。

## ROOT - 建立可共享的大模型任务上下文
- Position: 2162,70
- Size: 400,420
- Completion: 进行中
- Problem: 如何让人类快速看懂、纠正并持续维护大模型的当前工作状态？
- Approach: 用多树分层背景、架构和当前方法；用 MCP、逐轮 hook、flow 与 step evidence 形成读-做-写-验闭环。
- Input: task-trees.json # 树职责；task-tree.md # 当前方法
- Output: 可视任务图、执行流程和可审计证据。
- Metrics: 用户 30 秒内能判断焦点、有效方法、阻塞和下一动作。
- CurrentResult: 已建立 background/method/architecture 三树注册；只有 active method tree 绑定 flow、chain 和持续迭代。

## N1 - 设计 Markdown 节点格式
- Position: 459,474
- Size: 400,720
- Completion: 已完成
- Problem: Markdown 如何同时保持人可读、Agent 可写和前端可解析？
- Approach: 稳定契约迁至 trees/architecture.md 的 A1；本节点仅保留方法入口。
- CurrentResult: 固定短字段、二元边、GraphState、I/O 样例和 versions 分层已成为当前 schema。
- NextIdea: schema 契约变化时更新 A1，并同步本引用。

## N2 - 实现可视化图谱编辑器
- Position: 869,474
- Size: 400,420
- Completion: 进行中
- Problem: 如何一眼看清关系并直接编辑，同时保持 Markdown 为唯一数据源？
- Approach: 稳定 UI/API 架构迁至 trees/architecture.md 的 A2；本节点只保留未验收项。
- CurrentResult: 图编辑、自动保存、版本回退、I/O 快照、skill 推荐、多树切换、紧凑排版和双视图已上线。
- Notes: 节点删除按钮与双视图 SVG 的人工验收尚未记录；仅在用户重新聚焦 N2 时执行，不能从旧 NextPlan 触发。

## N4 - 分析本地 skill 仓库与自动调用机制
- Position: 2974,474
- Size: 470,750
- Completion: 已完成
- Problem: 如何从项目、kit 与全局目录自动选择最相关且不重复的 skill？
- Approach: 按节点意图召回，过滤泛词，按名称/描述/来源去重；展示用途、亮点和命中原因。
- Metrics: 同名或同描述仅出现一次；候选直接说明用途、亮点和命中依据。
- CurrentResult: N4 实测只返回 skill-creator/ask-matt/skill-installer/write-a-skill；多份 task-tree-grill 仅保留项目版。
- RootCauseAnalysis: 重复来自多目录副本，泛词 skill 造成无关召回；现按来源优先级折叠并过滤泛词。
- NextIdea: 收集用户接受/拒绝记录，评估是否需要项目级意图权重。
- SelectedSkills: codex:skill-creator

## N6 - 节点内多模型协作
- Position: 70,961
- Size: 400,420
- Completion: 进行中
- Problem: [子树] 节点级多模型对话、检索与持久化如何闭环？
- CurrentResult: 对话已按轮持久化；run 前可自动检索，模型仍可追加 search。
- NextIdea: 重启服务后跑一轮并重开浏览器，验证历史与自动检索记录仍在。
- Folded: true
- SubtreeFile: subtrees/N6-subtree.md
- SubtreeCount: 1

## N10 - 执行顺序视图（Scratch 模块流原型）
- Position: 1548,961
- Size: 520,640
- Completion: 已完成
- Problem: 如何把执行顺序与关系图分离并保留节点级证据？
- Approach: 稳定 flow 架构迁至 trees/architecture.md 的 A10；scripts/*.json 是顺序权威。
- CurrentResult: 双视图、Inspector 与 drift API 已上线；step evidence 由 scripts/steps 保存。
- NextIdea: 仅在执行语义变化时更新 A10 和 flow 协议。

## N9 - 可移植任务树 Kit 打包
- Position: 1015,961
- Size: 520,640
- Completion: 已完成
- Problem: 如何把任务图能力复制到其它项目且不覆盖既有 AGENTS.md？
- Approach: 稳定安装/隔离架构迁至 trees/architecture.md 的 A9；kit 合并带标记入口并隔离项目数据。
- CurrentResult: 一键更新实测 discovered=25、stub-refreshed=25、prompts-synced=25、failed=0。
- NextIdea: 新环境安装失败时先按 A9 的隔离与备份契约诊断。

## N3 - 让 Codex 同步维护任务图
- Position: 2539,474
- Size: 424,915
- Completion: 进行中
- Problem: [子树] 如何让 Agent 主动维护 compact current state，而不是追加过程历史？
- CurrentResult: 精炼协议已覆盖短字段、旧方法替换、树/flow 检查；N11 继续把自觉遵守升级为逐轮上下文和机械门禁。
- RootCauseAnalysis: 早期 Prompt 把活树当过程日志，导致旧方法、删除标记和新方案并存；历史应由 versions 保存。
- NextIdea: 维护机制的当前迭代统一在 N11，详细历史见子树。
- Folded: true
- SubtreeFile: subtrees/N3-subtree.md
- SubtreeCount: 1

## N5 - 处理任务树回溯后的文件漂移
- Position: 2078,961
- Size: 523,640
- Completion: 已完成
- Problem: 树回溯但代码和产物未回滚时，如何避免误判完成或误删文件？
- Approach: 恢复后的树是权威状态；未被树表示的产物只作 drift evidence，按当前节点重新验证。
- Metrics: 不因 orphan artifact 跳过任务；复用、重写或清理均由当前节点要求决定。
- CurrentResult: AGENTS 已规定回溯权威与漂移处理；task-tree-grill 已拆成短入口和按需 references。
- RootCauseAnalysis: 任务状态与文件产物独立持久化，恢复树不会恢复工作区。
- NextIdea: 出现真实 restore 时记录最小 drift，并重新验证当前 NextIdea。
- SelectedSkills: codex:skill-creator, agents:write-a-skill

## ST-P1 - 子树试点A chain-step 审计
- Position: 3455,474
- Size: 380,280
- Completion: 已完成
- Problem: [子树] chain-step 是否泄露后续 Chain？
- CurrentResult: P0 redaction 已合入；证据见 docs/subtree-parallel/findings-P1.md。
- Folded: true
- SubtreeFile: subtrees/ST-P1-subtree.md
- SubtreeCount: 1

## ST-P2 - 子树试点B 并行冲突梳理
- Position: 3865,474
- Size: 380,280
- Completion: 已完成
- Problem: [子树] 多 Agent 写树和代码有哪些冲突点？
- CurrentResult: 已完成冲突清单；证据见 docs/subtree-parallel/findings-P2.md 与 coordinator-run-1.md。
- Folded: true
- SubtreeFile: subtrees/ST-P2-subtree.md
- SubtreeCount: 1

## N11 - 重构多树上下文与 Agent 维护闭环
- Position: 2613,961
- Size: 0,720
- Completion: 进行中
- Problem: 如何保持树和 Prompt 精简，并让 Agent 在真实工作中及时维护树与执行流程？
- Approach:
  - AGENTS 短入口路由完整协议；background/architecture/method 分树，method <=12 KiB。
  - 每轮注入实时 Next；工作单元后立即写树，NextPlan 永不执行。
  - 2-4 个只读 worker 并发；写集预检后由唯一 coordinator 落盘，报告仅进本地记录。
- Input:
  - turn/completed -> agentMessage.text # worker 结果
  - {nodeId:"N2",writeSet:["public/**"]} # 租约样例
  - global-every-turn.en.md # 逐轮规则
- Output:
  - server/codex-coordinator.js # 租约与单写者协调
  - POST /api/codex/parallel -> {run:{id,status:"queued"}}
  - .task-tree-runs/<id>.json # 本地证据
  - public/index.html # 并发配置 UI
- Metrics: 并发峰值>=2；冲突写集调用模型前拒绝；仅 coordinator 可写；全套回归与桌面/390px UI 通过。
- Notes: 迁移前配置备份在两项目 .task-tree-maintenance/migration-backup/20260806-context-migration；未覆盖 task-tree.md。
- CurrentResult: huangyu Codex: plugin 0.7.0 installed/enabled; MCP handshake=17 tools+1 UI; project hook works. Installer now passes explicit paths and runs plugin add; Linux e2e passed.
- RootCauseAnalysis: Installer cwd hid the project stub; config enabled did not install plugin on Codex 0.134.0. Explicit entry/marketplace plus plugin add fixes both.
- CaseStudy:
  - stale NextIdea 指向已有产物 -> 先核实并回写，避免重做。
  - public/** 与 public/app.js 分租 -> 预检拒绝冲突。
- NextIdea: Sync fixed installer to huangyu, run bash -n, start demo service and curl its API; then check compact and flow.

## N7 - 集成 Markdown 知识库检索面板
- Position: 482,961
- Size: 520,420
- Completion: 已完成
- Problem: 如何索引本地 Markdown、检索/问答并把证据送入节点级多模型？
- Approach: 分块 embedding 后做向量+词法检索，按 path 去重；可合并联网搜索并在模型 run 前预检索。
- Metrics: 返回 path/分数/引用；空本地索引不阻断联网；长文不垄断 top-K。
- CurrentResult: batch=64、concurrency=40；默认每篇 1 段；索引原子写并支持变更块复用。
- RootCauseAnalysis: 长文多 chunk 会占满 top-K，串行 embedding 吞吐低；现用 per-doc 上限和批并发。
- NextIdea: 需要时增加按文件夹划分、可单选/多选的独立知识库。

## N12 - 让 Agent 直接调用任务图（MCP 集成层）
- Position: 3023,961
- Size: 0,720
- Completion: 已完成
- Problem: 如何让 Agent 结构化读写任务图，并让图界面反向续接 Codex 会话？
- Approach:
  - stdio MCP 复用 server runtime；写操作继承备份、焦点保护和 flow 同步。
  - MCP Apps 内联 public 资源，/api 请求经 task_tree_api 桥接。
  - app-server 按项目固定会话执行实时 next/chain preset；插件包自带 runtime。
- Input: scripts/mcp-server.mjs # 工具前门；marketplace/plugins/task-tree # 分发包
- Output: 17 个 task_tree 工具 + ui://task-tree/graph.html；server/codex-run.js # 反向会话
- Metrics: 包内入口可独立启动；工具/资源与文档对账；不同项目按 cwd 和固定端口隔离。
- Notes: 插件必须声明 mcpServers -> ./.mcp.json；MCP Apps 需 enable_mcp_apps 和 text/html;profile=mcp-app。
- CurrentResult: 0.7.0 包含 25 个 runtime 文件（754 KB）、17 工具和 1 个 UI 资源；故障注入可拦截清单、文件名和 bundle 漂移。
- RootCauseAnalysis: 旧包只声明能力却未启动包内入口验证，导致工具缺失且静默；现把真实启动与文档对账设为构建断言。
- NextIdea: 在第二台电脑验证 0.7.0 不依赖全局 kit 注册，并检查同名注册并存行为。

# GraphState
- ChainForceNext: 
- Current: N4
- Next: N11
- NextPlan: 重新打开任务图（Ctrl+F5）后验证：节点 × 在九宫格右上角；点 ⤓ 关系图/流程图 下载 SVG 并用 Inkscape/浏览器打开确认文字完整。

# Edges

## E1 - 格式支撑
- Endpoints: ROOT, N1
- Label: schema 支撑共享状态。

## E2 - 实现路径
- Endpoints: ROOT, N2
- Label: 编辑器呈现并修改共享状态。

## E3 - 协作规则
- Endpoints: ROOT, N3
- Label: Agent 维护协议。

## E4 - skill 调用扩展
- Endpoints: ROOT, N4
- Label: 节点意图路由 skill。

## E6 - 多模型协作扩展
- Endpoints: N2, N6
- Label: 节点级多模型交互。

## E7 - 知识库检索扩展
- Endpoints: N2, N7
- Label: 本地知识进入节点上下文。

## E8 - 检索送入多模型协作
- Endpoints: N6, N7
- Label: 检索结果进入多模型分析。

## E10 - 泳道原型
- Endpoints: N2, N10
- Label: 执行顺序视图。

## E9 - 可移植 Kit
- Endpoints: N2, N9
- Label: 编辑器能力可分发。

## E11 - Kit 同步协议
- Endpoints: N3, N9
- Label: Agent 协议随 kit 分发。

## E5 - 回溯一致性约束
- Endpoints: N3, N5
- Label: restore 后按树重新验证。

## E-ST-P1 - 子树试点A
- Endpoints: ROOT, ST-P1
- Label: chain 上下文审计。

## E-ST-P2 - 子树试点B
- Endpoints: ROOT, ST-P2
- Label: 并行冲突试点。

## E12 - 分层上下文与机械门禁
- Endpoints: N3, N11
- Label: 从长提示转为逐轮闭环。

## E13 - 项目流程与 Agent 生命周期
- Endpoints: N10, N11
- Label: flow 顺序与逐轮维护分层。

## E14 - MCP 前门复用维护闭环
- Endpoints: N11, N12
- Label: MCP 复用备份、门禁和 flow。

## E15 - 协议从自觉遵守转为可调用
- Endpoints: N3, N12
- Label: 提示协议由工具执行。
