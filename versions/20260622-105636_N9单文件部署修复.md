# LLM Task Graph

> 这个文件是大模型和前端共同维护的任务图。节点保存问题空间，边保存节点之间的关系；边可以连接两个或多个节点。
## ROOT - 建立可共享的大模型任务上下文

- Position: 1777,70
- Size: 400,720
- Completion: 进行中
- Problem: 人类看不到大模型隐性上下文，难以判断任务推进到哪里、是否偏航、卡点是什么。
- Approach: 用 Markdown 作为共享存储，用可视化前端把任务图展示出来，并允许直接编辑节点、边和批注。
- Input: 长对话、任务目标、模型阶段性发现、用户判断。
- Output: 可维护的任务图、节点内容、边关系、评价指标和批注。
- Metrics: 人类能在 30 秒内判断当前焦点、关系结构、阻塞点和下一步。
- Notes: 初始目标是让上下文外置，而不是让模型每轮输出长篇总结。
- CurrentResult:
- RootCauseAnalysis:
- CaseStudy:
- NextIdea:
- SelectedSkills:

## N1 - 设计 Markdown 节点格式

- Position: 2633,950
- Size: 400,720
- Completion:
- Problem: Markdown 需要同时适合人读、模型写、前端解析。
- Approach: 使用固定字段列表，节点标题包含 ID 和标题，边关系放到独立 Edges 区域。
- Input: 节点 ID、标题、位置、问题、思路、输入、输出、指标、批注。
- Output: 一个稳定的 Markdown schema。
- Metrics: 手写不费劲；前端可以解析并保存。
- Notes: 节点不再承担状态和父子关系，关系交给边表达。
- CurrentResult: Schema 保留 Input/Output、SelectedSkills、Completion 等机器可读字段，但前端把重字段移出主卡片，减少图谱视觉负担。本轮估算维护成本：整棵 task-tree.md 约 9.7k tokens，AGENTS.md 约 5.3k tokens；只读 GraphState + Next 节点约 943 tokens。
- RootCauseAnalysis: 字段膨胀的根因是任务状态、执行结果、失败原因和案例都写进同一节点后长期累积；应保留字段，但每轮只写 1-3 句结果和真正根因，下一步思路放 NextIdea/NextPlan，不塞进 RootCauseAnalysis。
- CaseStudy: case: 用户要求“节点里面删掉输入输出”，如果按字面删除字段，会失去真实样例和路径；按意图理解应是从主卡片移出，避免遮挡图谱判断。 case: 用户要求“完成态按钮”，如果实现成下拉框，会偏离用户要的快速切换；正确交互是动作区对勾按钮，按一次完成，再按取消。
- NextIdea:
- SelectedSkills:

## N2 - 实现可视化图谱编辑器

- Position: 3258,950
- Size: 670,819
- Completion:
- Problem: 需要一眼看图，并能直接在图上修改节点和关系。
- Approach:
  - 做本地 Web 应用，节点可拖动，连接点可拖线形成边，边可编辑。
  - 图谱编辑会自动保存回 task-tree.md，避免前端状态和 Codex 状态分叉。
  - 单击节点用于选择和拖动，双击节点才进入编辑模式，降低误编辑和难拖动的问题。
  - 前端轮询 task-tree.md，外部修改后会自动刷新图谱。
  - 边标签默认用于拖动摆放，双击边标签才进入编辑模式。
  - 边标签现在是边的控制点，拖动标签会带动边的弯曲中心，标签随边和节点移动。
  - 工具栏提供一键树形排版，将当前图整理为自上而下的标准树结构：根节点在顶部，直接子节点在下一层横向展开。
  - 节点连接点放在节点标题正下方，符合自上而下的树形连接方向。
  - 支持标记当前推进节点和下一步推进节点，并高亮从根节点到目标节点的整条路径。
  - 节点支持 Size 字段和右下角拖拽调整大小；节点内滚轮滚动内容，不再误触发画布缩放。
  - 树形排版按自上而下层级组织，父节点在上，直接子节点在下一层横向展开。
  - 服务端在写入和回退 task-tree.md 前创建版本快照，版本名带“将增加/修改/删除/回退”的原因。
  - 页面右侧增加版本树，显示每个版本的修改原因和时间；点击版本节点会回退，且回退前保留当前版本。
  - 节点主卡片不再显示 Input/Output；单击节点时，在节点左侧弹出同高 I/O 预览框，分别展示输入和输出示例。
  - I/O 预览长内容截取前 400 个字符；如果字段内容是工作区内路径，可以点击通过 /api/file 查看原文件。
  - 为每个项目增加双击入口：打开任务图.cmd 会自动查找或启动本项目服务、分配可用端口，并以应用窗口打开界面。
  - 服务端提供 /api/project，launcher 用它判断某个端口是否属于当前项目，避免多个项目共用固定端口造成混乱。
  - 下一步推进节点的“下一步”输入框下方增加 skill 推荐按钮；点击后根据当前节点和下一步思路检索本地 SKILL.md，在节点右侧显示同高能力面板，可多选并保存到 SelectedSkills。
  - 前端自动保存不再触发版本备份，避免用户在网页里每次输入都生成一个版本；版本备份仍由 Codex 手动改 task-tree.md 或执行回退时创建。
  - I/O 预览框和 skill 推荐面板都增加可拖动标题栏；内容区继续负责滚动、点击、勾选，避免移动浮层时误触发编辑。
- Input: task-tree.md。
- Output: 浏览器中的任务图谱编辑器。
- Metrics:
  - 能新增节点、拖动节点、连接节点、编辑边、保存 Markdown。
  - 修改节点或边后无需手动点击保存，也能落盘到 task-tree.md。
  - 外部修改 task-tree.md 后，前端能在短时间内同步显示。
  - 边标签能拖动到合适位置，并保存 LabelOffset。
  - 边标签不被节点遮挡；拖动边标签时边也随之变形。
  - 点击树形排版按钮后，节点不重叠，并按从上到下的层级排列。
  - 能一眼看出当前推进路径和下一步候选路径。
  - 能在右侧看到版本原因，点击任一版本可回退，回退不会删除历史版本。
  - 选中节点时能看到左侧 I/O 预览，长文本不撑爆节点，路径可点击打开。
  - I/O 预览框和 skill 面板能被拖到不遮挡节点的位置。
  - I/O 预览路径从整段文本中提取 Markdown 链接、引号路径、Windows 绝对路径和工作区相对路径，避免把说明文字当成路径。
  - 双击项目内的打开任务图.cmd 即可进入对应项目界面，无需记端口或手动 npm start。
- Notes: 第一版先做本地单人使用，不做多人冲突合并。
- CurrentResult: 图谱编辑器已支持节点/边/超边编辑、自动保存、版本回退、I/O 预览、skill 面板、模型协作入口和项目级启动器。本轮新增“关闭后台服务”按钮和 `/api/shutdown`：真实测试中 5192 服务可被关闭；重新运行 `open-task-tree.ps1` 后，服务在 57104 端口恢复，open-webSearch 3210 自动恢复，知识库仍有 1552 chunks。I/O 路径抽取也已修复并用真实 Markdown 文件验证。
- RootCauseAnalysis: 入口和后台进程问题的根因是任务图从“网页”演化成了“本地长期服务”，但原设计没有生命周期管理；关闭浏览器不等于关闭 Node/open-webSearch。版本、Completion、I/O 等问题的共同根因是前端完整重写 Markdown，而字段和交互不断扩展，必须由服务端保护关键字段并显式管理运行状态。
- CaseStudy:
  - case 1: 最初用左侧目录树和右侧编辑器，用户实际需要的是知识图谱式画布，因此界面抽象错位。
  - case 2: 用 Parent 表达关系，用户实际需要连接点拖线、边可编辑和超边，因此关系模型过窄。
  - case 3: 边标签先被实现成自由便签，用户实际需要边控制点，因此标签和边的语义没有绑定。
  - case 4: 新节点设为下一步后不高亮到根，说明路径搜索不能只按单向边假设，应该按图连通性回溯。
  - case 5: 如果 Codex 直接改 task-tree.md 而没有版本快照，用户只能看到最终状态，看不到为什么改、也不敢回退；因此版本节点必须显示修改原因，并且回退前也要备份当前状态。
  - case 6: 把输入/输出放在主节点中会让节点过高、图谱扫描困难；把它们移到选中节点旁的预览框后，平时看结构，需要时看真实样例。
  - case 7: 用户在图里已经写好了下一步，但还要切回聊天窗口复制，会打断工作流；因此 NextPlan 需要成为可执行入口，而不仅是备注文本。
  - case 8: 从网页直接启动 Codex 涉及权限、工作目录、会话归属和 VS Code 插件历史同步；如果这些边界不透明，用户会更难判断谁在改文件，因此该功能已删除，避免制造新的隐性状态。
  - case 9: 如果每个项目都要求手动 npm start 和记端口，任务图本身又会变成一个需要管理的任务；项目内双击入口把端口选择和复用隐藏起来，更符合“一个项目一个界面”的心智模型。
  - case 10: I/O 预览和 skill 面板如果只能固定在节点两侧，会在密集图谱中遮挡其它节点；借鉴“节点单击拖动、双击编辑”的原则，把移动行为限定到标题栏，内容区仍然正常滚动和交互。
  - case 11: 用户要“完成按钮”和“大对勾”，如果只新增 Completion 下拉框，虽然数据可表达，但交互不符合图谱快速判断需求。
  - case 12: 新字段加入后，已打开的旧前端自动保存会丢弃未知字段；因此服务端保存路径需要保护字段，不能完全信任客户端提交的是完整 schema。
  - case 13: 如果版本树每次点击旧版本都自动备份当前版本，会把“浏览/回退版本”变成制造新版本，导致版本树迅速膨胀；因此回退操作不再默认创建备份。
  - case 14: I/O 字段里常写成“路径：xxx（用于测试）”或 Markdown 链接；如果只取第一行整行当路径，/api/file 会收到带说明文字的无效路径。
- NextIdea: 下一步可以把 launcher 做成可复制模板，或者增加“创建桌面快捷方式”的脚本。
- SelectedSkills:

## N3 - 让 Codex 同步维护任务图

- Position: 2052,950
- Size: 400,720
- Completion:
- Problem: 如果 Codex 完成任务后只在对话里说明，图谱和真实任务进展会再次分叉。
- Approach:
  - 在 AGENTS.md 中加入完整的树维护协议，覆盖任务开始、任务结束、字段更新、版本备份、skill 路由、检查清单。
  - 协议按 §1~§8 组织：开始前读树/建树/定 Current/Next、结束后备份并更新 CurrentResult/RootCauseAnalysis/CaseStudy/Input/Output/NextIdea、GraphState、边关系。
  - 每个实质任务开始时先读取 AGENTS.md 和 task-tree.md；如果没有 task-tree.md，就先按 schema 建立任务树。
  - 对宽泛任务先建立或修订整体树，再按 Current/Next/NextPlan 逐节点推进，不一次性执行完整棵树。
  - 当用户请求改变研究方向时，重新判断 Current/Next；必要时新增节点和边，而不是硬塞进旧节点。
  - 开始执行时读取 GraphState.Next/Current 节点的 SelectedSkills；如果有选择，则解析全局 skill 并记录实际使用到 skill-routing-log.md。
  - 当真实输入源或输出物发生变化时，同步更新节点 Input/Output，避免任务图显示旧上下文。
- Input: 当前任务进展、用户反馈、实现变更、task-tree.md 最新内容。
- Output: 与实际工作进度一致的任务图。
- Metrics:
  - Codex 每次完成实质性任务后，task-tree.md 中对应节点或边有最小必要更新。
  - 新任务进入时不会绕过任务树；没有树则建树，有树则先读树。
  - 宽泛任务会先形成可见分解，再逐节点推进。
  - Current、Next、NextPlan 能反映当前研究方向。
- Notes: 规则要求不要使用 active/done/blocked 这类节点状态，而是用节点内容和边关系表达局面。
- CurrentResult: AGENTS.md 已重写为完整树维护协议：开始前读树、建树、定 Current/Next、检查 SelectedSkills；结束后备份并更新节点字段、GraphState 和边；包含字段写作指南、Markdown schema、skill-routing-log 协议、版本备份规则、回溯/漂移规则和结束前检查清单。现在大模型不仅能更新树的某个字段，还能按协议覆盖所有更新点。已继续补充回溯/漂移规则：task-tree.md 回溯后，以树为权威状态；已有文件只能当证据或草稿，不能让 Codex 跳过当前树要求的工作。已按回溯后的当前树重新验证并修正 `skills/task-tree-grill/SKILL.md`，让它专门用于一开始建树、长对话恢复上下文、回溯后处理文件漂移，并补充了 `agents/openai.yaml`。
- RootCauseAnalysis: 之前的 AGENTS.md 只说了完成后更新 task-tree.md，但没说具体更新哪些字段、怎么更新、什么时候更新、怎么写才合格。模型可能只更新了 CurrentResult 而漏掉 Input/Output，或者只改 GraphState 而没备份。根因是协议太模糊，缺少字段级指令和好/坏示例。新协议把每个字段的更新时机、内容要求、格式范例都写明了，并加了检查清单。回溯问题的根因是 task-tree.md 是状态回滚，文件系统不是事务型回滚；如果不明确权威来源，模型会把旧聊天记忆和残留文件误当成当前任务状态。
- CaseStudy:
  - case 1: 之前 N4 的 Input 还是三个本地仓库，但实际已改为全局 skill 目录，说明 AGENTS.md 没有强制要求 Input/Output 随数据源变化同步更新。
  - case 2: 之前更新节点时有时备份有时不备份，因为 AGENTS.md 只说备份再编辑但没有规定备份时机和命名格式。
  - case 3: CurrentResult 经常写得太笼统（如已实现 XXX），缺少具体文件、变更和效果，因为没有好/坏示例对比。
  - case 4: 任务完成后没有提哪个节点变了，用户看不到图谱变化，因为协议没有要求报告变更。
  - case 5: task-tree.md 回溯后没有旧 N5，但磁盘仍有 skills/task-tree-grill/SKILL.md 和 skill-routing-log.md 的旧记录；如果模型按记忆认为“已经做过”，就会绕过恢复后的真实任务状态。
- NextIdea: 可以做一个模板复制脚本，把 AGENTS.md + task-tree.md 空模板 + launcher 一键复制到新项目。
- SelectedSkills: codex:skill-creator

## N4 - 分析本地 skill 仓库与自动调用机制

- Position: 130,950
- Size: 460,720
- Completion:
- Problem: 用户在本项目下放入多个 skill 仓库，需要知道每个 skill 的用途、特点、调用方式，以及如何让大模型自动选择合适 skill。
- Approach: 扫描 CL4R1T4S、gill_with_doc、scientific-agent-skills 中的技能定义与说明，结合 tool/skill routing 相关研究，形成可落地的自动选择机制。
- Input: 全局 skill 根目录（~/.codex/skills、~/.agents/skills、~/.orchestra/skills）、当前节点 SelectedSkills、GraphState.NextPlan、节点 Problem/Approach/Metrics、skill-routing-log.md。
- Output: 中文 skill 推荐面板、SelectedSkills 落盘字段、AGENTS.md 执行协议、skill-routing-log.md 实际使用记录、更新后的节点 Input/Output。
- Metrics: 能列全本地 skill；能说明每类 skill 何时使用；能给出可实现的路由流程、置信度策略和评估办法。
- Notes: 这是一项新的分析任务，和已有任务图工具实现相关但不等同于前端功能开发。
- CurrentResult: 已扫描三个仓库并生成 skill-repos-analysis.md。CL4R1T4S 没有标准 SKILL.md，是提示词/工具档案库；gill_with_doc 有 34 个标准 skill，其中插件主清单暴露 17 个主力工程协作 skill；scientific-agent-skills 有 147 个标准 skill，已在分析文档中完整覆盖。已实现本地 /api/skills/recommend：根据当前节点和下一步思路检索标准 SKILL.md，并在前端右侧能力面板中多选保存到 SelectedSkills。推荐器已改为扫描全局 skill 根目录：~/.codex/skills、~/.agents/skills、~/.orchestra/skills，不再依赖项目下的三个仓库；返回给界面的能力说明已改为中文摘要。已补全调用链条：前端选择后显示已写入任务图，AGENTS.md 要求 Codex 读取 SelectedSkills 并把实际使用写入 skill-routing-log.md。
- RootCauseAnalysis: 不能把“本地有很多 skill”直接等同于“模型会正确自动调用”。根因是 skill 数量大、粒度不一、信任等级不同、触发边界相互重叠；如果把所有 SKILL.md 全塞上下文，会造成 token 浪费和误调用。更稳的方式是先索引 metadata，再按任务图和用户请求召回候选，最后只加载被选中的 skill。之前只扫项目内固定仓库会造成索引不完整，也会让每个项目重复配置 skill；现在改为全局根目录扫描。
- CaseStudy: case 1: CL4R1T4S README 含有要求模型泄露自身指令的攻击性文本，说明参考库不能自动进入执行池。 case 2: gill_with_doc 的 README 明确区分 user-invoked 和 model-invoked skill，说明自动调用应尊重 skill 的调用层级。 case 3: scientific-agent-skills 有 147 个 skill 和大量 references/scripts，说明必须用 progressive disclosure，不能一次性读全。 case 4: 前端自动保存如果每次都触发版本备份，会让版本树被用户输入噪声淹没；备份应该保留给 Codex 修改和显式回退这类关键状态变化。 case 5: 全局目录中诊断能力叫 diagnose 而不是 diagnosing-bugs，说明路由不能只依赖某个仓库的命名。 case 6: 只把 SelectedSkills 写入 Markdown 还不等于“已使用 skill”；必须有 AGENTS 协议和 skill-routing-log.md，把选择、解析、实际使用、结果串起来。
- NextIdea: 可以继续做 skill 启用/隐藏清单，减少推荐噪声；也可以增加一个“复制给 Codex 的下一步执行提示”按钮，把 NextPlan、SelectedSkills 和当前节点摘要组合成短提示。
- SelectedSkills: codex:skill-creator

## N5 - 处理任务树回溯后的文件漂移

- Position: 1990,1830
- Size: 523,640
- Completion: 已完成
- Problem: task-tree.md 可以通过版本树回溯，但代码、skill 文件、日志和其它产物不会自动回滚，导致“任务树说没做过、文件系统却残留做过的结果”。
- Approach:
  - 在 AGENTS.md 中明确 task-tree.md 是权威任务状态，聊天记忆、旧日志和残留文件只能作为证据。
  - 回溯后如果当前树没有记录某个产物，Codex 不能因为该产物存在就跳过任务；必须按当前 GraphState.Next 重新验证或重做。
  - 残留产物先视为 orphan artifact，不自动删除；只有当前节点要求时才复用、重写或清理。
- Input: task-tree.md、versions/、AGENTS.md、当前文件系统中的残留产物和 skill-routing-log.md。
- Output: AGENTS.md 中的 Rollback And Drift Rules、任务树中的漂移记录、下一步重新执行策略。
- Metrics: 回溯 task-tree.md 后，Codex 能明确按树继续推进；不会把不存在于当前树的旧节点当成已完成状态；文件漂移会被显式记录。
- Notes: 当前已发现一个具体漂移：task-tree.md 回溯后没有旧 N5，但 skills/task-tree-grill/SKILL.md 和 skill-routing-log.md 仍保留旧创建记录。
- CurrentResult: 已在 AGENTS.md 新增回溯/漂移规则：task-tree.md 是权威任务状态；已有文件、聊天记忆和日志只能作为证据；回溯后遇到已存在产物也要按当前节点重新验证或重做。随后按该规则重新验证并修正了旧的 `skills/task-tree-grill/SKILL.md`，没有直接把旧 N5 记录当成完成状态。现在已完成建树 skill 的重构：主 `SKILL.md` 缩短为入口流程，新增 `references/interview-playbook.md`、`references/graph-quality.md`、`references/schema-template.md`、`references/rollback-drift.md`，并更新 `agents/openai.yaml`。
- RootCauseAnalysis: task-tree.md 的版本回退只改变任务图，不会事务性回滚整个工作区。根因是任务状态和文件产物使用不同存储机制，没有统一快照；因此必须用协议规定权威来源和漂移处理方式。建树 skill 之前的问题是把访谈流程、schema、质量标准、回溯处理都塞在一个 SKILL.md 里，触发后上下文负担偏重；根因是没有按 skill-creator/write-a-skill 的 progressive disclosure 原则拆分核心流程和细节参考。
- CaseStudy:
  - case 1: 旧的建树 skill 文件仍存在，但当前 task-tree.md 没有对应节点记录；如果直接认为它已完成，就会违背回溯后的任务状态。
  - case 2: skill-routing-log.md 记录旧 N5，不代表当前树仍有 N5；日志是审计证据，不是任务状态源。
  - case 3: 如果建树 skill 把所有规则都放在主文件里，模型每次触发都会加载过多内容；拆成 references 后，只有需要访谈、质量检查、schema 或回溯处理时才加载对应文件。
- NextIdea: 下一步用一个真实的新任务或长对话片段试运行 `task-tree-grill`，检查它能否只问一个关键问题并产出可执行的 3-7 节点树。
- SelectedSkills: codex:skill-creator, agents:write-a-skill

## N6 - 节点内多模型协作

- Position: 1350,950
- Size: 520,720
- Completion: 进行中
- Problem: 用户希望在每个任务图节点内配置多个不同模型，并让它们基于整棵 task-tree.md 和各自历史/agent.md 独立思考当前节点问题。
- Approach:
  - 在节点标题下方增加同宽“模型协作”栏，打开右侧同高面板。
  - 本地保存多个模型配置：base_url、api_key、model、wireApi、启用状态；API key 不写入 task-tree.md。
  - 每个模型有独立 `model-agents/<id>.md` 作为 agent prompt，并在 `model-agent-history.json` 中保存该模型自己的节点历史。
  - 运行时把整棵 task-tree.md、当前节点 Markdown、用户问题、该模型历史和 agent prompt 一起发送给模型；多个模型并发独立回答。
  - 当前改为每个模型独立拥有临时会话上下文；用户可以只勾选一个模型进行多轮对话，也可以勾选多个模型并行对话。
  - 每个模型不再被动接收同一批统一检索结果，而是在服务端工具循环中自行决定是否请求检索；服务端按它给出的 search JSON 调用本地知识库和联网搜索，再把结果回填给该模型。
  - 每个模型运行时会收到页面内其它模型围绕同一节点的临时共享上下文，但这些内容不落盘、不写入 task-tree.md，刷新页面后消失。
- Input: task-tree.md、当前节点、用户填写的问题、model-agents.json、model-agent-history.json、model-agents/<id>.md、OpenAI-compatible base_url/api_key/model。
- Output: 节点内多模型协作栏、模型配置面板、并发运行接口、模型独立回答和本地历史记录。
- Metrics:
  - 可以在网页中新增/保存多个模型配置，不暴露 api_key 到 task-tree.md。
  - 每个节点能打开同宽模型协作栏并勾选多个模型运行。
  - 每个模型看到整棵树、当前节点、自己的 agent.md、自己的临时会话历史，以及其它模型在当前页面内的临时共享上下文。
  - 每个模型能自己决定要检索什么关键词，而不是只能看同一批预检索上下文。
  - 运行结果按模型分开展示，失败模型显示明确错误。
- Notes: 当前实现先做独立并排回答，不做 judge 融合；融合/裁判可以作为下一步扩展。
- CurrentResult: 多模型协作已支持 `.env` 配置、独立 agent.md、节点内并发回答、页面内临时多轮、跨模型临时共享上下文，以及模型自主 search 工具循环。模型输出只显示在页面，不写入 task-tree.md；已用 N7 真实问题验证 main 两轮对话、3 次自主检索、critic 读取 main 临时上下文。本轮新增树快照标记：`/api/model-agents/run` 在开始时读取 task-tree.md，返回 `treeSnapshotHash` 和 `treeChangedDuringRun`。
- RootCauseAnalysis: 多模型协作的核心隐患是隐藏记忆边界：持久历史会让用户不知道后续模型看过什么，运行中重读 task-tree.md 又会受用户正在编辑的内容干扰。因此当前采用页面内临时记忆 + 运行开始时树快照，刷新即清空，运行中不再读磁盘树。
- CaseStudy:
  - case 1: 如果把 api_key 写进 task-tree.md，模型和前端都能看到密钥，违背任务图作为共享上下文的边界；因此密钥只进 gitignored 的 model-agents.json。
  - case 2: 如果多个模型共享同一段历史，会破坏“独立专家”效果；因此历史按 modelId 分开保存，并只在对应模型 prompt 中注入。
  - case 3: 如果只给当前节点，不给整棵树，模型会失去任务全局背景；因此运行接口直接读取最新 task-tree.md 作为权威上下文。
  - case 4: 如果模型回答写入历史文件，用户会误以为“只看本次”的协作结果会长期影响后续模型判断；因此本轮改为不保存历史、不写任务树，只在当前 UI 中展示。
  - case 5: 统一按用户问题预检索再广播给所有模型，会让模型失去“自己判断该查什么”的能力；因此改成由模型请求 search，服务端执行检索并回填结果。
  - case 6: 如果关闭/重新打开面板就丢失对话，无法和单个模型多轮澄清；如果写入磁盘又会形成长期隐性记忆。当前折中是页面内临时会话，刷新页面后消失。
  - case 7: 如果不同模型完全看不到彼此临时对话，用户会在“多模型一起想”后仍要手工转述上下文；如果写入磁盘，又会形成用户不想要的长期隐性记忆。当前折中是页面内 sharedHistories。
- NextIdea: 下一步可以增加一个“融合裁判模型”，把多个模型回答归纳成共识、分歧、盲点和推荐写回节点 CurrentResult/NextIdea。
- SelectedSkills:

## N7 - 集成 Markdown 知识库检索面板

- Position: 740,977
- Size: 520,720
- Completion: 进行中
- Problem: 用户有 Obsidian/Copilot 风格的 Markdown 知识库，希望在任务图页面左侧直接建立 embedding 索引、检索文档、基于文档问模型，并让多模型协作能看到检索到的文献/笔记内容。
- Approach:
  - 在页面最左侧加入约五分之一宽度的知识库面板，包含知识库文件夹、embedding base_url/api_key/model、问答模型、重建索引、检索和问答入口。
  - 后端实现独立的 OpenAI-compatible embedding 索引：递归扫描默认 `knowledge/` 下的 `.md` 文件，按段落/标题分块，批量请求 `/embeddings`，保存到 gitignored 的 `knowledge-index.json`。
  - 检索时对问题重新 embedding，使用余弦相似度返回 top chunks；问答时把检索片段提供给已配置的聊天模型。
  - 多模型协作运行时可附带知识库面板最近检索到的片段，让多个模型同时看到同一批文献上下文。
  - 现有 `copilot/.obsidian` 索引只作为实现参考，不直接混用；因为 Copilot 索引文件巨大且 query embedding 必须与其 embedding 模型/维度一致。
  - 增加项目根目录 `.env` 一次性配置：多模型、每个模型的 agent.md 路径、embedding、知识库目录和 web search provider/API key 都只从 `.env` 读取。
  - 增加独立 web search provider 层，先支持 Tavily、Brave Search 和 Exa；搜索结果与本地知识库结果合并后进入知识库问答和多模型协作上下文。
  - 前端不再提供 API key、base_url、model 等配置表单，只显示 `.env` 配置状态，避免占用节点和面板空间。
  - 多模型协作运行时允许模型自主请求检索；服务端按模型请求调用本地知识库，并可按页面勾选同时调用 web search。
  - web search 增加 `searxng` provider：不需要 API key，但需要 `.env` 配置可访问且开放 JSON 输出的 SearXNG 实例。
  - web search 增加 `openwebsearch` provider：通过本地 `open-webSearch` daemon 的 `/search` 做 no-key 联网检索。
  - `打开任务图.cmd` 启动项目时会检查 `.env`，如果 `WEB_SEARCH_PROVIDER=openwebsearch` 且 daemon 未运行，会尝试自动启动 `open-webSearch/build/index.js serve`。
  - web search 配置返回 `requiresApiKey`，前端对 `openwebsearch`/`searxng` 显示“不需要 key”，不再误报“缺少 key”。
- Input: `knowledge/` 下的 Markdown 文档、`.env`、`knowledge-config.json`、`web-search-config.json`、`model-agents.json`、`model-agents/<id>.md`、当前知识库检索问题、多模型协作问题。
- Output: 左侧知识库面板、`.env.example`、`/api/knowledge/config`、`/api/knowledge/reindex`、`/api/knowledge/search`、`/api/knowledge/ask`、`/api/web-search/config`、`/api/web-search/search`、多模型 prompt 中可选注入的本地知识库和网页搜索片段、gitignored 的本地配置和索引文件。
- Metrics:
  - 用户无需打开 Obsidian，也能把 Markdown 放进默认文件夹后从页面重建索引。
  - 检索结果显示来源路径、相似度和片段内容。
  - 问答模型基于检索片段回答，并能指出上下文不足。
  - 多模型协作可选择附带最近检索片段。
  - 模型、embedding、web search 和 agent.md 路径只通过 `.env` 配置一次后复用。
  - 联网搜索结果显示来源、URL、分数和片段内容，并能和本地知识库结果一起进入模型上下文。
  - API key 不进入 `task-tree.md`。
  - 多模型协作点击运行后，即使左侧没有最近检索结果，也会按模型问题检索本地知识库并注入上下文。
  - `WEB_SEARCH_PROVIDER=searxng` 时不要求 `WEB_SEARCH_API_KEY`，但缺少 `WEB_SEARCH_BASE_URL` 会给出明确错误。
  - `WEB_SEARCH_PROVIDER=openwebsearch` 时不要求 `WEB_SEARCH_API_KEY`，并能通过本地 daemon 返回真实网页搜索结果。
  - 双击 `打开任务图.cmd` 后，如果 open-webSearch 已完成依赖安装和 build，联网搜索能力会随项目入口一起可用。
  - 知识库索引保存在 `knowledge-index.json`，建立一次后可复用；新增或修改 Markdown 后点击“重建索引”会全量重建并继续复用。
  - 点击“重建索引”后显示进度条和阶段；已有 chunk embedding 会按内容 ID 复用，只有新增或修改片段重新请求 embedding。
- Notes: 第一版没有实现真正联网搜索；当前 OpenAI-compatible chat 接口本身不保证 web browsing。要联网，需要后续增加明确的搜索 API 或 provider-specific web-search tool 配置。可选方案分两类：模型厂商内置搜索工具（如 OpenAI Responses web_search、Perplexity Sonar）和独立搜索 API（如 Tavily、Exa、Brave Search）。更通用的项目方案是先接独立搜索 API，把网页结果作为上下文喂给任意模型；厂商内置搜索适合单一模型链路。
- CurrentResult: 本地 Markdown RAG 已支持 `.env` 配置、索引重建、检索/问答、联网搜索和多模型自主 search。已修复索引端到端问题：损坏的 `knowledge-index.json` 已备份为 `knowledge-index.corrupt-backup.json` 并修复，保留 1552 个 embedding；索引写入改为原子替换；重建索引显示进度并复用未变化 chunk。真实测试：重建到 100%，RestGPT/ToolGen 查询命中对应 Markdown 第一位，knowledge+web 混合检索可用。用户决定不在任务图内集成 Cursor Agent 启动按钮；探索性对话与执行继续在 Cursor 桌面端进行，任务图只维护 task-tree.md。
- RootCauseAnalysis: 知识库“看起来没有索引”的根因不是没有文档，而是 139MB 索引文件尾部损坏，服务端解析失败后静默回退为空索引。另一个根因是全量重建依赖外部 embedding 服务，服务负载高会失败；因此必须原子写入、保留旧索引、按 chunk 内容复用 embedding。
- CaseStudy:
  - case 1: `copilot/.obsidian` 里已有 8 个 50MB 以上的索引分区；如果页面每次启动都全量读取，会让任务图入口变慢。
  - case 2: Copilot metadata 显示 embedding 是 `vector[3072]`；如果用户配置 1536 维 embedding 模型查询，余弦相似度无法计算。
  - case 3: 用户要求“模型能联网”，但当前 OpenAI-compatible `/chat/completions` 调用只发送文本，不自动拥有搜索工具；因此必须明确标注未实现联网，而不是在 UI 或 prompt 中暗示已联网。
  - case 4: 如果把 api_key 继续只放在网页表单或 JSON 中，多项目复用会变成重复配置；`.env` 让模型、embedding 和 web search 的密钥都在本地一次性声明。
  - case 5: 如果让每个模型共用同一个 agent prompt，会破坏模型协作里的角色差异；因此每个模型继续使用独立 `model-agents/<id>.md` 或 `.env` 指定的 agent 文件。
  - case 6: 网页里继续展示 base_url/api_key/model 会占用节点和侧栏空间，而且制造“可以在多个地方配置”的隐性状态；移除表单后，配置来源只剩 `.env`。
  - case 7: 用户以为 web search 应该是一个工具，但本地 Node 应用不能调用 Codex 会话注入的 `web` tool；因此项目内必须使用独立搜索 provider，或者接一个 no-key 的 SearXNG 实例。
  - case 8: 如果多模型协作只附带左侧“最近检索片段”，用户忘记先检索时模型仍然看不到知识库；因此模型运行接口需要支持模型主动触发检索。
  - case 9: npm 初次安装失败留下残缺 `node_modules`，导致 daemon 启动时缺 `setprototypeof`、`unpipe`；用项目内 cache + 代理执行 `npm ci` 才恢复了完整依赖树。
  - case 10: 当前树未记录 openwebsearch，但磁盘已有实现；如果直接按记忆跳过测试，会违背树回溯规则，因此本轮重新执行了真实联网测试。
  - case 11: `openwebsearch` 本来不需要 key，但前端只根据 `hasApiKey` 显示状态，导致误报“缺少 key”；应由后端返回 `requiresApiKey` 让 UI 区分 no-key provider。
  - case 12: `knowledge-index.json` 有 139MB，但 JSON 末尾残留了额外数字，导致服务端解析失败后静默回退为空索引；根因是大文件直接写入不是原子操作，半写/残留会让后续检索看起来像“没有建立索引”。
  - case 13: 全量重建 1552 个 chunk 时 embedding 服务返回 “Service load is too high”；如果没有复用旧 embedding，用户每次更新一个 md 都要承担整库失败风险。
- NextIdea: 下一步可以在页面知识库面板显示 open-webSearch daemon 状态和失败原因，避免用户只看到“联网失败”而不知道是 daemon 未启动、依赖未 build，还是搜索引擎限流。
- SelectedSkills:

## N9 - 可移植任务树 Kit 打包

- Position: 3180,1830
- Size: 520,640
- Completion: 已完成
- Problem: 其他项目也要用同一套任务图能力，且各自维护独立的 task-tree.md；复制过去的手续要少，已有 AGENTS.md 的项目不能被覆盖。
- Approach:
  - 改 server/launcher 支持 task-tree.config.json 的 projectRoot，kit 放子目录、数据放项目根。
  - 导出 llm-task-tree-kit/ 文件夹，复制到任意项目后运行 install.ps1 即可。
  - install 创建 task-tree.starter.md、合并 AGENTS.md 标记块、追加 gitignore；完整协议保留在 AGENTS.task-tree.md。
  - 改前全量备份到 backups/snapshot_*。
- Input: 当前仓库 server.js、public/、AGENTS.md、skills/task-tree-grill、启动脚本。
- Output: llm-task-tree-kit/、task-tree.config.json、scripts/build-kit.ps1、backups/snapshot_20260621-195906/。
- Metrics: 复制 kit 到新项目 → install → 打开任务图 → 独立 task-tree.md；已有 AGENTS.md 只追加 llm-task-tree 块不覆盖。
- Notes: open-webSearch 等大依赖仍可选，不强制打进 kit。
- CurrentResult: 已备份至 backups/snapshot_20260621-195906/。server.js 与 open-task-tree.ps1 支持 projectRoot；新增 llm-task-tree-kit/（install.ps1、templates、AGENTS.task-tree.md、task-tree-grill）。根目录 task-tree.config.json 保持 projectRoot=. 兼容本仓库。新增一键部署三件套：setup-task-tree.cmd + setup-task-tree.ps1 + setup-task-tree.kitpath；复制到目标项目根后双击 .cmd 即可同步 kit、install、打开任务图；e2e-simulation/deploy-test-project 实测通过。
- RootCauseAnalysis: 原先所有路径绑在 __dirname，kit 作为子目录时无法让 task-tree.md 落在项目根；根因是未区分 kitDir 与 projectRoot。一键脚本若硬编码 Unicode 路径会在 PowerShell 解析失败，因此改用 setup-task-tree.kitpath 单行文件或环境变量指向 kit 源。
- CaseStudy:
  - case 1: 目标项目已有 AGENTS.md 时，整文件替换会破坏原有规则；因此 install 只追加 <!-- llm-task-tree:begin/end --> 块。
  - case 2: 用户要求改前备份；因此先 snapshot 再改 server 与新增 kit。
- NextIdea: 可选：install 时自动把 task-tree-grill 复制到项目 ./skills/；或提供 zip 分发包含已填好 kitpath 的三件套。
- SelectedSkills:

# GraphState

- Current: N9
- Next: N7
- NextPlan: 评估本地 agent-reach 包能否接入知识库/联网检索，让模型自主决定搜索哪些来源。

# Edges

## E1 - 格式支撑

- Endpoints: ROOT, N1
- LabelOffset:
- Label: 格式支撑
- Notes:

## E2 - 实现路径

- Endpoints: ROOT, N2
- LabelOffset:
- Label: 实现路径
- Notes:

## E3 - 协作规则

- Endpoints: ROOT, N3
- LabelOffset:
- Label: 协作规则
- Notes: 前端自动保存解决人改图后的落盘问题；AGENTS.md 解决 Codex 完成任务后的同步问题。

## E4 - skill 调用扩展

- Endpoints: ROOT, N4
- LabelOffset:
- Label: skill 调用扩展
- Notes: 用户希望把本地 skill 仓库纳入模型工作流，并进一步解决模型自动选择合适技能的问题。

## E5 - 回溯一致性约束

- Endpoints: N3, N5
- LabelOffset: -11,257
- Label: 回溯一致性约束
- Notes: N5 是 N3 的协议补充，解决 task-tree.md 回溯后文件系统残留导致 Codex 误判任务已完成的问题。

## E6 - 多模型协作扩展

- Endpoints: ROOT, N2, N6
- LabelOffset:
- Label: 多模型协作扩展
- Notes: N6 扩展 N2 的节点交互能力，让用户可在图谱节点内调用多个外部模型独立分析当前问题。

## E7 - 知识库检索扩展

- Endpoints: ROOT, N2, N6, N7
- LabelOffset:
- Label: 知识库检索扩展
- Notes: N7 扩展页面入口和多模型协作上下文，让本地 Markdown 知识库检索结果能进入节点级模型分析。

## E9 - 可移植 Kit

- Endpoints: ROOT, N2, N3, N9
- LabelOffset:
- Label: 可移植 Kit
- Notes: N9 产出 llm-task-tree-kit，供其它项目复制安装独立任务树。
