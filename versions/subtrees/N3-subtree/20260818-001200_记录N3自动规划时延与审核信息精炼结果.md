# LLM Task Graph Subtree

> Fold root: N3
> v2 并行试跑包

## N3 - 让 Codex 同步维护任务图
- Position: 2052,950
- Size: 400,420
- Completion: 进行中
- Problem: 多个 Agent 共用一棵树时，如何避免把全局 Next 误当作各自目标并互相覆盖？
- Approach:
  - 模型结合本轮目标、ROOT 与未决节点生成2–4个独立分支；留空目标时才只依赖任务树。
  - 程序校验依赖、独占写集、保留路径与环；有依赖时只运行可执行前沿。
  - worker 在独立 worktree 写入并测试，coordinator 在 integration 中核验、修复与集成。
  - 全部分支完成、集成测试通过且有改动才可接受；失败分支可原地修订并局部重跑。
- Input: 当前任务树、本轮目标、可审核并行草案，以及开始与结束两次用户决定。
- Output: 带目标的自动规划、隔离并行、严格接受门禁、失败分支局部重跑与接受后节点同步。
- Metrics: 草案阶段零写入；越界或写集冲突必拒绝；任何失败、阻塞或集成测试失败都锁定接受；局部重跑不重复成功分支；主工作区并发修改不被覆盖。
- Notes:
- CodeLoc:
- CurrentResult: 自动并行现支持显式本轮目标、失败或阻塞时锁定接受，并只修订重跑失败分支；真实浏览器与 Git 验收确认正常路径仍为3次点击，局部重跑不重复成功分支，接受前主目录不变。自动并行可靠性目标已达到；长期语义维护仍需真实项目观察。
- RootCauseAnalysis: 规划输入此前缺少本轮目标，接受门禁只看集成测试与改动，失败后只能整批丢弃。现由目标透传、全分支成功门禁和复用 integration worktree 的局部重跑共同约束。
- CaseStudy:
  - case 1: 主工作区已有未提交和未跟踪文件 → 快照保留用户基线，审核前主目录不变。
  - case 2: 运行期间用户再次修改同一文件 → 接受被阻断，不静默覆盖。
- NextIdea: 在真实业务项目使用自动并行，观察接受后任务树摘要是否扣住根本目标，据此精炼状态同步 Prompt。
- SelectedSkills: codex:skill-creator

# GraphState

- Current: N3
- Next: N3
- NextPlan: v2 findings 已完成；可选 sync-stub 或真实任务试跑 AGENTS 协议

# Edges
